import CONST from '../../constants/index.js';

export default class LayoutView {
	constructor(scene, currentLayout) {
		this.scene = scene;
		this.currentLayout = currentLayout;

		this.draw();
	}

	draw() {

	}
}
