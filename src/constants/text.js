const textLocation = 'data/text';

const texts = {
	left: {
		key: 'left',
		location: `${textLocation}/left.json`,
	},
	middle: {
		key: 'middle',
		location: `${textLocation}/middle.json`,
	},
	right: {
		key: 'right',
		location: `${textLocation}/right.json`,
	},
};

export default texts;